server:
  port: 8087

spring:
  application:
    name: document-service

  datasource:
    url: ${DOCUMENT_DB_URL:jdbc:postgresql://localhost:5439/document_db}
    username: ${DOCUMENT_DB_USERNAME:postgres}
    password: ${DOCUMENT_DB_PASSWORD:postgres}

  jpa:
    hibernate:
      ddl-auto: none

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
