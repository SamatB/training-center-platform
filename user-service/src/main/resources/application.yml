spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: user

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

server:
  port: 8081